#include<stdio.h>
#include<stdlib.h>

struct node{
	int data;
	struct node *next;
};

int main(){
	
	int n,i,value;
	struct node *temp = NULL, *head =NULL, *newnode;
	printf("enter the number of nodes:");
	scanf("%d",&n);
	for(i=0;)
	
	return 0;
}
